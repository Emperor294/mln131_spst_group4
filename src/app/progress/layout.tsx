import "@/features/course/course.css";
import "@/features/assessment/assessment.css";
import "@/features/assessment/progress-dashboard.css";

export default function ProgressLayout({ children }: { children: React.ReactNode }) {
  return children;
}
